{
  "name": "dbx-embedded-terminal-ui",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "build:ui": "esbuild ui-src/main.js --bundle --minify --format=iife --platform=browser --outfile=ui/app.js",
    "watch:ui": "npm run build:ui -- --watch"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0",
    "lucide": "1.47.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
